<% if drink.favorited_by?(current_user) %>
  <%= link_to drink_favorite_path(drink_id: drink.id), method: :delete, remote: true do %>
      ♥<%= drink.favorites.count %>
    <% end %>
  <% else %>
    <%= link_to drink_favorite_path(drink_id: drink.id), method: :post, remote: true do %>
    ♡<%= drink.favorites.count %>
  <% end %>
<% end %>